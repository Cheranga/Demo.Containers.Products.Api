name: "Demo.Containers"
concurrency: "Demo.Containers"
env: 
  APP_NAME: cherademoproductsapi
  RG_NAME: rg-chera-demo
  RG_LOCATION: australiasoutheast
  DOTNET_VERSION: 6.0.x
  PUBLISHED_PATH: ./published

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    
jobs: 
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install .NET Core
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
      - name: Restore projects
        run: dotnet restore
      - name: Build Projects      
        run: dotnet build --no-restore -c Release                
      - name: Generate Build Number
        id: generatebuildnumber
        run: |
          # echo ::set-env name=BUILD_VERSION::$(date +%Y%m%d_%H%M%S)
          # echo "BUILD_VERSION=$(date +%Y%m%d_%H%M%S)"
          echo '::set-output name=BUILD_VERSION::$(date +%Y%m%d_%H%M%S)'
      - name: Build Image and Publish
        uses: Cheranga/GitHubActions/buildandpushtodockerhub@master
        with:
          dockerUserName: ${{secrets.DOCKER_USER}}
          dockerPassword: ${{secrets.DOCKER_PASSWORD}}
          dockerFilePath: Demo.Containers.Products.Api/Dockerfile
          imageName: demoproductsapi
          imageTag: ${{env.BUILD_VERSION}}
          
      # - name: Login to Docker
      #   run: |
      #     docker login -u ${{secrets.DOCKER_USER}} -p ${{secrets.DOCKER_PASSWORD}}
      # - name: Build Docker Image
      #   run: |
      #     docker build -t ${{secrets.DOCKER_USER}}/demoproductsapi --file Demo.Containers.Products.Api/Dockerfile .
      # - name: Push to Docker
      #   run: |
      #     docker push ${{secrets.DOCKER_USER}}/demoproductsapi
            
           
            